#pragma once

#include <string>
#include <functional>

#include "constants.h"

long double HubbleRate(long double T);
long double omegah2(long double MS, long double lHS, long double TR);
long double lambda012(long double MS, long double TR);